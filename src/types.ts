export enum BrowserType {
    Chrome = 'Chrome',
    Brave = 'Brave',
    Firefox = 'Firefox',
    Edge = 'Edge',
}

export type Feature = {
    name: string
    description: string
    icon: string
}